@using Microsoft.AspNetCore.Html
@using TrainingGuides.Web.Features.LandingPages

@model TemplateViewModel<LandingPageTemplateProperties>

@{
    var templateModel = Model.GetTemplateModel<LandingPageViewModel>();
    ViewBag.Title = templateModel.Message;
    string tagText = Model.Properties.MessageType;
    HtmlString openingTag = new HtmlString($"<{tagText}>");
    HtmlString closingTag = new HtmlString($"</{tagText}>");
}

@openingTag
    @templateModel.Message
@closingTag


<editable-area area-identifier="areaMain" />

@section styles {
    <page-builder-styles/>
}
@section scripts {
    <page-builder-scripts/>
}